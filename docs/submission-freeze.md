# Submission version record (transparency note)

DoraHacks submission: **2026-07-08** (BUIDL "CrooCred", CROO Agent Hackathon).
This file exists so judges can distinguish exactly what was submitted from what
changed afterwards. We prefer disclosing post-deadline commits over pretending
the repo froze.

| What | Value |
| --- | --- |
| Submitted commit (HEAD on submission day) | `dab1310` — docs: finalize submission |
| Demo video (submitted 7/08) | https://youtu.be/wcp8gUcTvzo (2:56, public, 1080p — kept public, unchanged) |
| Demo video (current, v5) | https://youtu.be/J0rXME936TA (4:13, public, 1080p) — re-recorded 2026-07-10 and linked on 2026-07-11, within the DoraHacks submission-editing window; adds the Warranty parent/child-order case, the deadline snapshot, the correction story and architecture depth. Every number narrated is the deadline-cutoff figure; no post-deadline activity is claimed. |
| Live site | https://croocred.axiqo.xyz — **continuously live**; the daemon rebuilds it after every delivered order (that is the product working, not a resubmission) |
| Production deployment | esbuild bundles of the commits below, systemd `croocred` on the same host as the site |

## Post-deadline commits, in full

| Commit | Date | Nature | Why it exists |
| --- | --- | --- | --- |
| `58ab748` | 2026-07-09 | **Scoring-fairness bugfix** (docs'd + disclosed) | An external cross-tester's Re-Check orders exposed that our auto-synthesized probe input could fabricate placeholder data, get correctly rejected by the target, and unfairly brand the agent F. Fix: such runs are now `capOutcome: "not_placed"` and excluded from board grades. Publicly acknowledged and explained to the affected builder in the CROO Discord before the fix shipped. Leaving a known-unfair grading bug live through judging week seemed worse than a disclosed fix. |
| `8b34f23` | 2026-07-10 | Community arena feature | Axion Clash race handler (deterministic forecast service, min-price). Fulfils a pre-deadline commitment to another team; does not touch certification/scoring logic. |
| docs/data commits after 7/10 | 2026-07-10+ | Documentation + evidence snapshots only | README wording corrections (on-chain facts vs off-chain judgment), honest-metrics disclosure, this file, judge materials, and routine `evidence/` data snapshots (the evidence page states the directory is committed on every change; the sync resumed after a 2-day lag — records themselves are generated by the live daemon, not hand-edited). |

## What never changed post-deadline

- Certification pipeline, probe engine, provider order flow (except the
  disclosed fairness fix above), pricing, services, site design. The demo
  video was re-recorded (v5) within the editing window — disclosed above.

## Source-of-truth note

The judges' aggregated CAP order data (provided by CROO) supersedes all
self-reported numbers. Our live feeds (`/api/certs-full.json`,
`/api/verdicts.json`, `/api/stats.json`) exist to be cross-checked against it.
